# Scripts Directory

The `scripts/` directory contains automation for environment setup and repository maintenance.

- `bootstrap.sh` - scaffolds the lab structure and consolidates existing lab assets.
- `init-dev-env.sh` - initializes development dependencies and tooling.
- `run-juice-shop.sh` - launches the Juice Shop lab environment.
