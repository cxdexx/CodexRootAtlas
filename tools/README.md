# Tools Directory

The `tools/` folder contains helper tooling for environment setup, containerization, and automation.

- `docker/` - container definitions and helper assets.
- `scripts/` - tooling utilities and automation helpers.

Use this area for reusable infrastructure and pentest support tools.
